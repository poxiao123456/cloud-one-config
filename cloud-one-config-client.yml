spring:
  profiles:
    active: dev
---
spring:
  profiles: dev

config:
  version: dev_version_5
  
user:
  username: zhangsan5

---
spring:
  profiles: test

config:
  version: test_version

---
spring:
  profiles: pro

config:
  version: pro_version
