spring:
  profiles:
    active: dev
---
spring:
  profiles: dev

config:
  version: dev_version_2
  
user:
  username: zhangsan2

---
spring:
  profiles: test

config:
  version: test_version

---
spring:
  profiles: pro

config:
  version: pro_version
